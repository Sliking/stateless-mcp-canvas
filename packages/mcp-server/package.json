{
  "name": "stateless-mcp-demo-server",
  "version": "1.0.0",
  "description": "Truly stateless MCP server on Cloudflare Workers using MCP SDK v2 (SEP-1442, SEP-2322, SEP-2243)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0-beta.5",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.22.0"
  }
}
